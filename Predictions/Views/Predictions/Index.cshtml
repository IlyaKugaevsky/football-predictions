@model Predictions.ViewModels.PredictionsDisplayViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Прогнозы</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <div class="container">
            <div class="panel panel-default">
                <div class="panel-heading"></div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-3">
                            @*@Html.DropDownListFor(model => model.SelectedExpertId, 
                                                  new SelectList(Model.Expertlist, "ExpertId", "Nickname"),
                                                  new { @class = "selectpicker"})*@
                            <select name="@Html.NameFor(model => model.SelectedTourId)"
                                    id="@Html.IdFor(model => model.SelectedTourId)"
                                    class="selectpicker"
                                    data-width="auto"
                                    title="Выбрать эксперта">
                                @foreach (var item in Model.Tourlist)
                                {
                                    <option value="@item.TourId.ToString()">@item.TourId</option>
                                }
                            </select>
                        </div>        
                        <div class="col-md-2">
                            @*@Html.DropDownListFor(model => model.SelectedTourId, new SelectList(Model.Tourlist, "TourId", "TourId"),
                                                new { @class = "form-control", placeholder = "Дома" })*@
                            <select name="@Html.NameFor(model => model.SelectedTourId)"
                                    id="@Html.IdFor(model => model.SelectedTourId)"
                                    class="selectpicker"
                                    data-width="auto"
                                    title="Выбрать тур">
                                @foreach (var item in Model.Tourlist)
                                {
                                    <option value="@item.TourId.ToString()">@item.TourId</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="row top-buffer">
                        <table class="table table-hover">
                            <tbody>
                                <tr>
                                    <th class="col-md-2">Дата</th>
                                    <th class="col-md-1">Дома</th>
                                    <th class="col-md-1">В гостях</th>
                                    <th class="col-md-1">Прогноз</th>
                                </tr>

                                @if (Model.Tour != null)
                                {
                                    for (int i = 0; i <= Model.Tour.Matches.Count - 1; i++)
                                    {
                                        @Html.HiddenFor(model => Model.Tour.Matches[i].MatchId)
                                        <tr>
                                            <td class="col-md-2">@Model.Tour.Matches.ElementAt(i).Date</td>
                                            <td class="col-md-1">@Model.Tour.Matches.ElementAt(i).HomeTeam.Title</td>
                                            <td class="col-md-1">@Model.Tour.Matches.ElementAt(i).AwayTeam.Title</td>
                                            <td class="col-md-1">@Model.Tour.Matches.ElementAt(i).Predictions.FirstOrDefault().Value</td> @* TO FIX!!!!!!!!! *@

                                        </tr>
                                    }
                                }

                                <tr><td><input type="submit" class="btn btn-info" value="Показать прогноз"></td></tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            @*<h2>@Model.Tour.TourId Тур</h2>
            <div class="panel panel-default">
                <div class="panel-heading">@Model.Tour.StartDate - @Model.Tour.EndDate</div>
                <div class="panel-body">
                    <div class="col-md-5">
                        <table class="table table-hover">
                            <tbody>
                                <tr>
                                    <th class="col-md-2">Дата</th>
                                    <th class="col-md-1">Дома</th>
                                    <th class="col-md-1">В гостях</th>
                                    <th class="col-md-1">Прогноз</th>
                                </tr>
                                @for (int i = 0; i <= Model.Tour.Matches.Count - 1; i++)
                                {
                                    @Html.HiddenFor(model => Model.Tour.Matches[i].MatchId)
                                    <tr>
                                        <td class="col-md-2">@Model.Tour.Matches.ElementAt(i).Date</td>
                                        <td class="col-md-1">@Model.Tour.Matches.ElementAt(i).HomeTeam.Title</td>
                                        <td class="col-md-1">@Model.Tour.Matches.ElementAt(i).AwayTeam.Title</td>
                                        <td class="col-md-1">
                                            @Html.EditorFor(model => model.InputPredictionValuelist[i],
                                                new { htmlAttributes = new { @class = "form-control" } })
                                    </td>
                                </tr>
                                }
                                <tr><td><input type="submit" class="btn btn-info" value="Сохранить прогноз"></td></tr>
                            </tbody>
                        </table>
                    </div>
                    <div class="col-md-2">
                        @Html.DropDownListFor(model => model.SelectedExpertId, new SelectList(Model.Expertlist, "ExpertId", "Nickname"),
                                                new { @class = "form-control", placeholder = "Дома" })
                </div>
            </div>
            </div>*@
        </div>
    </div>
}